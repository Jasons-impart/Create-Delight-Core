# CDC Content Map

This file records implemented CDC features: what exists, how it behaves, where it lives, and its current status.

| Area | Visible or runtime behavior | Implementation outline | Main locations | Related notes | Status |
|---|---|---|---|---|---|
| Core registrations | CDC blocks, items, fluids, menus, recipe types, tags, and tabs register under `createdelightcore`. | Registries use `CreateDelightCore.REGISTRATE` and focused classes in `registry/`; content implementations live under `content/`. | `src/main/java/io/github/jasonsimpart/createdelightcore/registry/`, `src/main/java/io/github/jasonsimpart/createdelightcore/content/` | `src/main/java/io/github/jasonsimpart/createdelightcore/registry/AGENTS.md` | Active pattern. |
| Data generation | Generated assets/data are shipped with the mod because `src/generated/resources` is in main resources. | Providers are wired through `CDCoreDatagen` / `CDCoreDatagenEvent`; language uses `data/lang` handlers; recipe providers live under `data/recipe`. | `src/main/java/io/github/jasonsimpart/createdelightcore/data/`, `src/generated/resources/` | `src/main/java/io/github/jasonsimpart/createdelightcore/data/AGENTS.md` | Run after provider changes. |
| Compatibility patches | CDC integrates with JEI, Jade, Create Metallurgy, CMR, Fruit Delight, and many mod-specific behavior hooks. | Non-mixin integration belongs in `compat/`; invasive behavior changes belong in `mixin/` and must be listed in the mixin config. | `src/main/java/io/github/jasonsimpart/createdelightcore/compat/`, `src/main/java/io/github/jasonsimpart/createdelightcore/mixin/`, `src/main/resources/mixins.createdelightcore.json` | `src/main/java/io/github/jasonsimpart/createdelightcore/compat/AGENTS.md`, `src/main/java/io/github/jasonsimpart/createdelightcore/mixin/AGENTS.md` | Version-sensitive. |
| ExtraHoloPage 全息球翻页 | 每页显示 7 个附加入口，加上固定材料入口共 8 个选项；超出后显示上一页、下一页和页码，返回入口列表仍保留当前页。 | 客户端 Mixin 在构造 `HoloItemGui` 时写入页内坐标，再按每页 7 项控制可见性，避免原生取消选中动画把入口恢复到未分页坐标；`CombatMixinPlugin` 按 `extraholopage` 模组 ID 条件加载。 | `mixin/extraholopage/ExtraHoloItemsGuiMixin.java`、`mixin/CombatMixinPlugin.java`、`mixins.createdelightcore.json` | 目标版本为 ExtraHoloPage 1.2.16、Tetra 6.9.0；升级后复核构造器、`changeItem` 和 Mutil GUI API。 | 隔离 `clean build` 与本地运行 JAR 部署已通过；真实客户端回归已由用户确认。 |
| 战斗倍率兼容 | Apothic Attributes 超额暴击改用新版加算公式；Iron's Spells 回响打击读取伤害事件创建时的原始金额；Travel Optics 暗影瘴气对超过1倍的法术强度倍率使用渐近于5倍的递减公式。 | `LivingHurtEvent` 保存不可变伤害快照，三个目标模组 Mixin 由 `CombatMixinPlugin` 按模组 ID 条件加载；暗影瘴气从原 `getSpellPower` 结果还原倍率，再用 `1 + 4(m-1)/(m+3)` 计算有效倍率，施法和法术提示共用同一表达式修改。 | `compat/combat/`、`mixin/combat/`、`mixin/CombatMixinPlugin.java`、`CDConfig.java` | 整合包 `docs/attributeslib-traveloptics-echoing-strikes-analysis.md` | 已构建；目标模组存在和全部缺失两种客户端加载场景均已验证。 |
| CEI Tetra scroll printing | Create: Enchantment Industry printers copy a data-bearing `tetra:scroll_rolled` from one paper and 250 mB ink while preserving all scroll NBT. | CDC conditionally registers a `PrintEntry` through CEI's Forge event when both mods are loaded; empty scrolls are rejected and the output is a single full copy of the target. | `src/main/java/io/github/jasonsimpart/createdelightcore/compat/createenchantmentindustry/TetraScrollPrinterCompat.java`, `CreateDelightCore.java`, `build.gradle`, `src/main/resources/META-INF/mods.toml` | None. | Compiles and builds; in-game machine validation pending. |
| Sophisticated Backpacks × Sol Apple Pie 高级喂食 | 高级喂食升级在原有可喂食条件下，优先吃加入当前 FoodList 后 diversity 最高的食物；没有候选或自定义尝试失败时保持原逻辑。 | `AdvancedFeedingUpgradeMixin` 扫描升级存储，使用 `FoodList.simulateFoodAdd` 计算当前收益，并通过 `UpgradeWrapperBaseAccessor` 识别高级升级后调用原私有喂食方法。 | `src/main/java/io/github/jasonsimpart/createdelightcore/mixin/sophisticatedbackpacks/AdvancedFeedingUpgradeMixin.java`, `UpgradeWrapperBaseAccessor.java`, `src/main/resources/mixins.createdelightcore.json` | None. | 构建通过；含两模组的游戏内行为回归待完成。 |
| NBT 遗传培养液 | 单一 `createdelightcore:genetic_culture` 虚拟流体可按流体栈 NBT 显示不同颜色和名称，并在 JEI 中保持为不同子类型。 | `Color` 读取 RGB/ARGB 整数或十六进制字符串，`Name` 读取翻译键，`CustomName` 读取文本或 JSON Component，`Variant` 作为稳定分型；流体类型不创建空 NBT，避免渲染改变配方匹配结果。 | `registry/CDFluids.java`、`content/fluid/GeneticCultureFluidType.java`、`compat/jei/GeneticCultureFluidSubtypeInterpreter.java` | 整合包 `kubejs/server_scripts/Create Mob Spawners/recipes.js` | 已实现并通过编译，需进游戏验证 JEI 和管道表现。 |
| Fruit Delight custom fruit support | CDC can add jelly / jello fruit behavior without appending unsafe `FruitType.values()` entries. | Custom fruit lookup goes through `CustomFDFruits`; food classes live in `compat/fruitsdelight`; mixins stay generic and read stable fruit names from NBT. | `src/main/java/io/github/jasonsimpart/createdelightcore/compat/fruitsdelight/`, `src/main/java/io/github/jasonsimpart/createdelightcore/mixin/fruitsdelight/` | `docs/lessons-learned.md` | Known pitfall documented. |

## Entry Template

| Area | Visible or runtime behavior | Implementation outline | Main locations | Related notes | Status |
|---|---|---|---|---|---|
| `<feature>` | `<what changes at runtime>` | `<how it works>` | `<paths>` | `<docs or none>` | `<planned/active/needs validation>` |
